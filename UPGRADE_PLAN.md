# TypeCraft 升级文档

## 1. 文档目标

这份文档用于整理 TypeCraft 当前可优化和可升级的方向，给后续迭代提供统一参考。

重点不是一次性大改，而是按优先级分阶段推进，优先解决：

- 可用性问题
- 可维护性问题
- 易回归问题
- 后续功能扩展的结构阻力

---

## 2. 当前项目概况

TypeCraft 当前已经具备以下核心能力：

- 真人模拟打字
- Markdown 行类型识别
- 表格输入脚本
- 代码块输入模式
- 自动补全闭合符号跳过
- Tkinter 图形界面

当前主要问题不在“能不能跑”，而在：

- 文案和编码质量不稳定
- 测试缺失
- 配置模型松散
- UI 与核心逻辑耦合偏高
- 缺少面向后续扩展的结构约束

---

## 3. 升级优先级总览

### P0：必须优先处理

1. 修复乱码和编码问题
2. 清理 UI/README 文案一致性
3. 补最基本的自动化测试
4. 引入结构化配置模型

### P1：高价值升级

1. 增加编辑器预设
2. 支持配置保存/加载
3. 增强运行前校验
4. 拆分 UI 与核心引擎的耦合

### P2：体验和扩展升级

1. 重做 UI 视觉与信息层级
2. 增强日志与调试能力
3. 升级脚本系统
4. 做插件化或策略化输入适配

---

## 4. 详细升级项

### 4.1 编码与文案修复

#### 问题

当前仓库内存在多处中文乱码，影响：

- README 可读性
- 用户界面理解
- 日志定位
- 后续维护效率

#### 目标

- 所有源码文件统一为 UTF-8
- 所有界面文案、日志文案、提示文案可直接阅读
- README 与 UI 行为描述一致

#### 建议实施

1. 全量检查 `.py`、`.md` 文件编码
2. 统一转换为 UTF-8
3. 逐个修正：
   - `main.py`
   - `ui/app.py`
   - `ui/tabs/*`
   - `ui/dialogs/*`
   - `README.md`
4. 建立文案统一表，避免同一功能出现多种说法

#### 验收标准

- 终端读取源码不再出现乱码
- UI 中不再出现异常字符
- README 与程序行为一致

---

### 4.2 自动化测试补齐

#### 问题

当前核心逻辑几乎没有自动化测试，任何回滚、重构或新增功能都容易引入回归。

#### 目标

先覆盖最稳定、最值得测试的纯逻辑模块。

#### 第一批建议测试范围

- `TextProcessor.detect_line_type`
- `TextProcessor.parse_markdown_table_row`
- `TextProcessor.parse_auto_pairs`
- 列表/引用自动续写处理
- 代码块模式切换逻辑
- 表格分隔线跳过逻辑

#### 建议实施

1. 引入 `pytest`
2. 新建 `tests/`
3. 优先从 `core/processor.py` 开始补单测
4. 对 `TypingEngine` 关键分支做最小可测封装

#### 验收标准

- 至少有一组可稳定运行的测试
- 关键 Markdown 解析逻辑被覆盖
- 代码块模式和表格模式有回归保护

---

### 4.3 配置模型结构化

#### 问题

当前配置通过 `dict` 在 UI 和引擎之间传递，字段越来越多，后续维护成本会上升。

#### 风险

- key 拼写错误难发现
- 默认值分散
- 缺少类型约束
- 配置导入/导出困难

#### 目标

把配置升级为结构化模型。

#### 建议方案

使用 `dataclass`：

```python
@dataclass
class TypingConfig:
    system: str = "Windows"
    min_delay: float = 0.04
    max_delay: float = 0.10
    countdown: int = 5
    ...
```

#### 建议实施

1. 新建配置模型文件，例如 `core/config.py`
2. UI 输出 `TypingConfig`
3. 引擎和 handlers 全部改为读取属性，而不是读字典 key
4. 顺便统一默认值来源

#### 验收标准

- UI 不再拼装裸字典
- 引擎使用强类型配置
- 新增配置项只需改一处默认定义

---

### 4.4 运行前校验

#### 问题

当前很多配置错误只能在运行中暴露，用户体验差。

#### 建议增加的校验

- `min_delay <= max_delay`
- 倒计时非负
- 错字率范围合法
- 自动补全配置字符串格式合法
- 表格模式开启时脚本不能为空或给出提醒
- `block_editor` 模式下未配置退出脚本时给出警告

#### 实施建议

在 UI 层统一增加 `validate_config()`。

#### 验收标准

- 常见错误可在点击“开始执行”前被发现
- 错误信息可直接指导用户修正

---

### 4.5 编辑器预设

#### 目标

降低用户首次配置成本。

#### 建议预设

- Notion
- Obsidian
- 通用 Markdown
- 飞书文档

#### 每个预设应包含

- 列表自动续写开关
- 引用自动续写开关
- 表格模式开关
- 默认脚本
- 代码块模式

#### 验收标准

- 用户可以一键选择目标编辑器
- 多数场景下不需要从零配置

---

### 4.6 配置保存与加载

#### 目标

允许用户保留自己的输入参数和编辑器脚本配置。

#### 建议实现

- 导出 JSON 配置
- 导入 JSON 配置
- 自动记住最近一次配置

#### 延伸价值

- 与结构化配置模型天然匹配
- 后续可以做“预设 + 自定义配置库”

#### 验收标准

- 用户可以保存一套配置并重复使用
- 重启程序后可恢复最近一次配置

---

### 4.7 UI 与引擎解耦

#### 问题

当前 UI 直接把配置函数、日志函数、状态回调传给引擎，耦合度较高。

#### 目标

把界面层和执行层之间的边界明确下来。

#### 建议方向

- UI 负责收集配置和显示结果
- 引擎只接收配置对象和事件接口
- 日志、状态更新通过统一事件回调输出

#### 推荐结构

- `TypingConfig`
- `TypingEvent`
- `TypingEngine`
- `UI Controller`

#### 验收标准

- 核心引擎可以脱离 Tkinter 被调用
- 后续可扩展 CLI 或批量模式

---

### 4.8 脚本系统升级

#### 当前能力

当前脚本已经支持：

- 单键
- 组合键
- `delay:x`

#### 可升级方向

- 更明确的脚本语法
- 脚本解析报错定位
- 录制后预览
- 脚本校验
- 脚本示例模板

#### 中长期方向

把脚本系统从“字符串”升级为“可解析动作序列”。

例如：

```json
[
  {"type": "combo", "value": "tab"},
  {"type": "delay", "value": 0.3},
  {"type": "combo", "value": "shift+tab"}
]
```

#### 验收标准

- 录制结果更可控
- 错误脚本更容易定位
- 用户更容易理解和修改

---

### 4.9 UI 体验升级

#### 当前问题

- 默认 Tk 风格较重
- 页面信息层级弱
- 状态反馈不够强
- 参数区和执行区关系不清晰

#### 建议方向

- 顶部做明确的状态区
- 参数分组卡片化
- 日志区终端化
- 增加“当前目标编辑器模式”展示
- 增加“预计行为摘要”

#### 注意

UI 升级应排在编码修复和配置结构化之后，否则容易在不稳定基础上反复返工。

---

## 5. 分阶段落地建议

### 第一阶段：1 天内可完成

- 修复乱码
- 统一 README 和 UI 文案
- 增加运行前校验
- 清理显眼的状态/提示问题

### 第二阶段：2 到 3 天

- 引入 `TypingConfig`
- 增加基础测试
- 支持配置导入导出
- 增加编辑器预设

### 第三阶段：1 周左右

- 重构引擎与 UI 边界
- 升级脚本系统
- 重做 UI 视觉和信息架构
- 增强日志和调试能力

---

## 6. 推荐执行顺序

建议严格按下面顺序推进：

1. 编码与文案修复
2. 配置模型结构化
3. 自动化测试
4. 运行前校验
5. 编辑器预设
6. 配置保存/加载
7. UI 与引擎解耦
8. UI 视觉升级
9. 脚本系统升级

这个顺序的原因是：

- 先解决“看不清、容易错、难验证”
- 再解决“难扩展、难维护”
- 最后处理“更高级、更好看”

---

## 7. 建议的里程碑

### Milestone A：可维护版本

目标：

- 没有乱码
- 配置清晰
- 有基本测试
- 行为可验证

### Milestone B：可复用版本

目标：

- 有编辑器预设
- 有配置保存/加载
- 用户无需重复配置

### Milestone C：可扩展版本

目标：

- 引擎与 UI 解耦
- 脚本系统增强
- 能继续扩展更多编辑器适配

---

## 8. 结论

TypeCraft 当前已经有了一个不错的功能雏形，真正需要升级的不是单个按钮或单个模块，而是：

- 稳定性
- 可维护性
- 配置体系
- 验证能力
- 扩展边界

如果只追求“先加功能”，项目会越来越难改。
如果先把配置、测试和结构打牢，后面做 UI 升级、预设、脚本增强都会更顺。
